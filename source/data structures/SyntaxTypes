#pragma once


namespace SyntaxTypes {
    enum Type { Instruction, Block, Function, Operation, Class, Structure, Namespace };
    enum Memory { Global, Component, Static }; 
    enum Scope { Public, Private, Protected };
}

