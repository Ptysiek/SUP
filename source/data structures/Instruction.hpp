#pragma once

#include "iSyntax.hpp"


class Instruction : public iSyntax {
public:
    //enum Type { Instruction, Function, Operation, Class, Structure };
    //enum Memory { Global, Component, Static }; 

    //Type getSyntaxType() { return Type::Instruction; }
    //virtual Memory getMemoryType() = 0;

};
